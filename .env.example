# Copy to .env and fill in real values. .env is gitignored - never commit it.
# docker-compose reads this automatically and substitutes it into
# docker-compose.yml's ${VAR:-default} placeholders.

DARAJA_CONSUMER_KEY=
DARAJA_CONSUMER_SECRET=
DARAJA_PASSKEY=
DARAJA_SECURITY_CREDENTIAL=

FLW_SECRET_KEY=
FLW_WEBHOOK_SECRET_HASH=
